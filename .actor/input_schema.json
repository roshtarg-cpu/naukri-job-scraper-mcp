{
  "title": "Naukri.com Job Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchQuery": {
      "title": "Search Query",
      "type": "string",
      "description": "Job title or keywords to search for (e.g., 'software engineer', 'data scientist', 'product manager')",
      "prefill": "software engineer",
      "editor": "textfield"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "City or location to filter jobs (e.g., 'bangalore', 'mumbai', 'delhi'). Leave empty for all locations.",
      "prefill": "bangalore",
      "editor": "textfield"
    },
    "maxResults": {
      "title": "Maximum Results",
      "type": "integer",
      "description": "Maximum number of job listings to scrape",
      "default": 50,
      "prefill": 10,
      "minimum": 1,
      "maximum": 500,
      "editor": "number"
    }
  },
  "required": ["searchQuery"]
}
